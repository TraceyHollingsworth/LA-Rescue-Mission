<div class="container">
  <h3>Edit Question</h3>

  <%= render 'form' %>
  <%= link_to 'Delete', question_path(@question.id), class: "btn btn-danger btn-xs", method: :delete, question: { confirm: 'Are you sure?' } %>
</div>
